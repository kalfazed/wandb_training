- _target_: lightning.pytorch.loggers.CSVLogger
  save_dir: ${hydra:runtime.output_dir}
  name: csv
- _target_: lightning.pytorch.loggers.WandbLogger
  project: wandb_training
  save_dir: ${hydra:runtime.output_dir}
  log_model: false
